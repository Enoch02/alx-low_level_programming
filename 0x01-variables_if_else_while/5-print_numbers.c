#include <stdio.h>

/**
 * main - The main function
 * Description: prints 0 - 9
 * Return: Returns 0
 */
int main(void)
{
	int i = 0;

	for (int i = 0; i < 10; i++)
		printf("%d", i);

	printf("\n");
	return (0);
}
