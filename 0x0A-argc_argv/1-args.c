#include <stdio.h>

/**
 * main - check the code
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0.
 */
int main(int argc, __attribute__((__unused__)) char **argv)
{
	printf("%d\n", argc - 1);

	return (0);
}
